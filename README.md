# ChatPulse

[![CI](https://github.com/mishkacher/ChatPulse/actions/workflows/ci.yml/badge.svg)](https://github.com/mishkacher/ChatPulse/actions/workflows/ci.yml)
[![macOS 13+](https://img.shields.io/badge/macOS-13%2B-black?logo=apple)](https://github.com/mishkacher/ChatPulse)
[![Swift 6](https://img.shields.io/badge/Swift-6-orange?logo=swift)](https://www.swift.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

**ChatPulse** — нативная утилита для строки меню macOS, которая следит за выбранными чатами ChatGPT и автоматически отправляет команду:

> продолжай и не останавливайся до технического лимита

Приложение использует собственный встроенный браузер на системном движке **WebKit**. Google Chrome, Playwright, Ollama, платные API и отдельный ИИ-агент не нужны.

![Обзор интерфейса ChatPulse](docs/chatpulse-overview.svg)

## Статус проекта

Текущая версия: **0.3.0 alpha**.

- исходники опубликованы в основной ветке `main`;
- сборка и тесты выполняются в GitHub Actions на macOS 15;
- 20 автоматических проверок качества включены в CI;
- перед постоянным использованием требуется ручной приёмочный тест с реальным аккаунтом ChatGPT.

## Как выглядит приложение

В строке меню macOS находится значок ChatPulse. По нажатию открывается компактное меню:

- статус работы;
- одна команда **«Запустить / Остановить»**;
- ручная проверка;
- настройка интервала;
- открытие встроенного браузера;
- добавление текущего чата;
- список сохранённых чатов;
- журнал последних действий.

Отдельное окно **«Браузер ChatPulse»** похоже на упрощённый Safari. В верхней панели находятся:

- назад, вперёд, обновить и открыть ChatGPT;
- текущий адрес;
- меню **«Войти ▾»**;
- кнопка **«Добавить чат»**;
- строка состояния авторизации.

## Что реализовано

- встроенный `WKWebView` на WebKit;
- постоянное локальное хранение cookies и данных входа;
- отдельное видимое окно для входа и выбора чатов;
- отдельный скрытый WebKit-контроллер для фоновых проверок;
- вход по **email / одноразовому коду** через официальный экран OpenAI;
- вход с **passkey** через WebAuthn и системный интерфейс macOS;
- команда повторного запроса кода по email на поддерживаемом шаге проверки;
- ChatPulse не читает и не сохраняет email, коды, пароли или passkey;
- контролируемые всплывающие окна, которые не могут стать больше видимой области экрана;
- защита от перехода на `about:blank` вместо страницы авторизации;
- сохранение названий, URL и состояния чатов;
- интервалы 1, 2, 5, 10, 15 и 30 минут;
- собственный интервал от 30 секунд;
- **at-most-once** защита: один ответ не получает команду повторно даже при неопределённом DOM-подтверждении после клика;
- ожидание завершения ответа;
- пассивная первая проверка после запуска;
- подтверждение появления отправленного сообщения, когда интерфейс позволяет это проверить;
- сохранение пользовательских изменений даже во время долгой проверки;
- полностью русский пользовательский интерфейс и журнал;
- модульные тесты и автоматический аудит.

## Что удалено намеренно

Детектор технического лимита отсутствует. ChatPulse не анализирует баннеры или тексты лимитов и не пытается обходить ограничения ChatGPT.

Если поле ввода или кнопка отправки недоступны, команда не форсируется. Ошибка записывается в журнал, а следующая попытка происходит только при очередной плановой проверке.

## Главная логика

1. На первой проверке ChatPulse запоминает текущий ответ и ничего не отправляет.
2. При появлении нового ответа приложение снова только запоминает его.
3. Если на следующей проверке ответ не изменился, завершён и принадлежит ассистенту, отправляется команда продолжения.
4. До клика ошибки считаются безопасными для повторной попытки на следующем интервале.
5. После фактического клика отпечаток ответа помечается обработанным независимо от того, успел ли DOM подтвердить появление сообщения.
6. Один и тот же ответ не может получить команду дважды.
7. Перед отправкой повторно проверяется, что чат не был удалён или отключён пользователем.

## Требования

- macOS 13 Ventura или новее;
- подключение к интернету;
- аккаунт ChatGPT;
- Swift 6 / Xcode Command Line Tools только для сборки из исходников.

## Установка из исходников

Скопируйте весь блок целиком в Terminal:

```bash
cd ~
rm -rf "$HOME/ChatPulse-install"
git clone --depth 1 https://github.com/mishkacher/ChatPulse.git "$HOME/ChatPulse-install"
cd "$HOME/ChatPulse-install"
bash scripts/install_app.sh
```

Команда использует отдельную чистую папку `~/ChatPulse-install` и не создаёт вложенные копии `ChatPulse/ChatPulse`.

Приложение устанавливается в `/Applications/ChatPulse.app`, а без прав записи — в `~/Applications/ChatPulse.app`.

Для ручного запуска:

```bash
open "/Applications/ChatPulse.app" 2>/dev/null || open "$HOME/Applications/ChatPulse.app"
```

## Первое использование

1. Запустите ChatPulse.
2. Нажмите значок приложения в строке меню.
3. Выберите **«Открыть браузер ChatPulse…»**.
4. Нажмите **«Войти ▾»**.
5. Выберите **«Войти по email / коду»** или **«Войти с passkey»**.
6. После авторизации откройте нужный чат.
7. Нажмите **«Добавить чат»**.
8. Повторите для остальных чатов.
9. Выберите интервал и нажмите **«Запустить»**.

## Вход по email / коду

ChatPulse открывает официальный адрес `https://chatgpt.com/auth/login`, выбирает доступный email-сценарий и фокусирует поле email.

- одноразовый код отправляет OpenAI;
- код вводится только на странице OpenAI;
- ChatPulse не читает почту и не сохраняет введённые данные;
- пункт **«Запросить код по email на этой странице»** нажимает официальный вариант `Try with email`, когда он присутствует на текущем шаге;
- используйте самое новое письмо, поскольку предыдущий код может стать недействительным.

Если аккаунт изначально создан только через Google, OpenAI может не разрешить вход по email или сброс пароля. В таком случае используйте заранее добавленный passkey либо исходный социальный способ входа вне встроенного браузера.

## Вход с passkey

ChatPulse проверяет поддержку WebAuthn и ищет официальный вариант **Passkey / Ключ доступа** на странице OpenAI. Когда OpenAI запускает WebAuthn-запрос, WebKit передаёт его системному интерфейсу macOS.

В зависимости от настройки Mac подтверждение может происходить через:

- Touch ID;
- пароль пользователя macOS;
- iCloud Keychain;
- сторонний менеджер учётных данных;
- совместимый аппаратный ключ безопасности.

Passkey должен быть заранее добавлен в аккаунт OpenAI. Если он ещё не создан, войдите в ChatGPT через обычный Safari или официальное приложение, откройте настройки безопасности аккаунта и добавьте ключ доступа. После этого повторите вход в ChatPulse через **«Войти с passkey»**.

## Cookies и вход

Встроенный браузер использует постоянное хранилище WebKit. Авторизация сохраняется между запусками ChatPulse, но не связана с cookies, историей или вкладками Safari.

### Вход через Google

Google запрещает OAuth-авторизацию во встроенных пользовательских агентах, включая управляемые приложением WebView. ChatPulse распознаёт такой переход, закрывает дочернее окно и предлагает перейти к email-коду или passkey вместо огромной белой страницы.

Вход, выполненный отдельно в Safari, нельзя безопасно перенести в локальное хранилище `WKWebView` ChatPulse.

## Хранение настроек

```text
~/Library/Application Support/ChatPulse/settings.json
```

В настройках сохраняются:

- названия и URL чатов;
- включённое состояние;
- интервал проверки;
- отпечатки последних ответов;
- время последней отправки;
- результат подтверждения последней отправки.

Email, одноразовые коды, пароли, passkey и содержимое всей переписки в файл настроек не записываются.

## Проверка проекта

```bash
make test
make audit
make build
```

`make audit` запускает модульные тесты и 20 обязательных проверок проекта.

## Архитектура и документация

- [Архитектура](docs/ARCHITECTURE.md)
- [Установка и устранение неполадок](docs/SETUP.md)
- [План тестирования](docs/TEST_PLAN.md)
- [20 раундов оптимизации и аудита](docs/OPTIMIZATION_AUDIT.md)
- [Модель безопасности](SECURITY.md)
- [Правила участия](CONTRIBUTING.md)

## Ограничения alpha-версии

- интерфейс ChatGPT может измениться и потребовать обновления селекторов;
- вход и отправка должны быть проверены на реальном аккаунте;
- email-вход может быть недоступен аккаунтам, созданным только через социальный провайдер;
- passkey должен быть предварительно зарегистрирован в аккаунте OpenAI;
- Google OAuth внутри встроенного браузера намеренно не поддерживается;
- сборка подписывается локальной ad-hoc подписью, а не Apple Developer ID;
- приложение не обходит и не ускоряет сброс лимитов ChatGPT.

## Лицензия

MIT License. См. [LICENSE](LICENSE).
